import React from "react";
import styles from "./Footer.module.css";

const Footer = () => {
    return (
        <div className={styles.footer}>
            <p>Copyrights Créditos</p>
            <p>Veja o código desse repositório no GitHub</p>
        </div>
    );
};

export default Footer;
